# BTC-Crash-BOT — Disabled

This bot is **disabled**. It is commented out in crontab and no longer runs.

`crash_bot.py` is kept for reference only.

The hybrid dispatcher (`HYBRID-DCA-BOT/dispatcher.py`) covers all buying logic.
See `HYBRID-DCA-BOT/CONTEXT.md` for current setup documentation.
